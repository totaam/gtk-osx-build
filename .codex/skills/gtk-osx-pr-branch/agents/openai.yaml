interface:
  display_name: "GTK-OSX PR Branch"
  short_description: "Create gtk-osx-build PR branches"
  default_prompt: "Use $gtk-osx-pr-branch to create a gtk-osx-build.totaam update branch, apply an update across applicable modulesets, commit it, and push it."
